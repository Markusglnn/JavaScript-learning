let zahl1 = 5;
let zahl2 = 10;
let summe = zahl1 + zahl2;
console.log("Die Summe von " + zahl1 + " und " + zahl2 + " ist: " + summe);

let produkt= [2, 3, 4];
let ergebnis = produkt[0] * produkt[1] * produkt[2];
console.log("Das Produkt der Zahlen " + produkt[0] + ", " + produkt[1] + " und " + produkt[2] + " ist: " + ergebnis);